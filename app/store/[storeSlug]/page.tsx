"use client";

import Link from "next/link";
import { Check, Minus, Plus, Search, ShoppingBag, Sparkles, Store, X } from "lucide-react";
import { useParams } from "next/navigation";
import { useMemo, useState } from "react";
import { Logo } from "@/components/logo";
import { useApp } from "@/components/app-provider";

export default function StorefrontPage() {
  const { storeSlug } = useParams<{ storeSlug: string }>();
  const { state, addToCart, setCartQuantity } = useApp();
  const [category, setCategory] = useState("All");
  const [search, setSearch] = useState("");
  const [drawer, setDrawer] = useState(false);
  const categories = ["All", ...new Set(state.listings.filter((listing) => listing.isPublished).map((listing) => state.inventory.find((item) => item.id === listing.inventoryItemId)?.category).filter(Boolean) as string[])];
  const products = useMemo(() => state.listings.filter((listing) => {
    const item = state.inventory.find((entry) => entry.id === listing.inventoryItemId);
    return listing.isPublished && item && item.quantity > 0 && (category === "All" || item.category === category) && listing.title.toLowerCase().includes(search.toLowerCase());
  }), [state.listings, state.inventory, category, search]);
  const cartDetails = state.cart.flatMap((cartItem) => {
    const listing = state.listings.find((item) => item.id === cartItem.listingId);
    return listing ? [{ ...cartItem, listing }] : [];
  });
  const total = cartDetails.reduce((sum, item) => sum + item.listing.price * item.quantity, 0);
  if (storeSlug !== state.business.slug) return <div className="empty"><h2>Store not found</h2><Link href="/" className="btn btn-primary">Go home</Link></div>;

  return <div style={{ minHeight: "100vh", background: "#fafbf9" }}>
    <header className="store-header"><Logo /><nav><Link href="/dashboard">Seller dashboard</Link><button className="cart-button" onClick={() => setDrawer(true)}><ShoppingBag size={18} />Cart {state.cart.length > 0 && <b>{state.cart.reduce((sum, item) => sum + item.quantity, 0)}</b>}</button></nav></header>
    <section className="store-hero"><div><span className="store-kicker"><Store size={14} />Local pickup available</span><h1>{state.business.name}</h1><p>Professional care products, selected by people who know hair.</p><div className="store-meta"><span><Check size={14} />Pay at pickup</span><span><Check size={14} />Local service</span></div></div><div className="store-mark">{state.business.name.split(" ").map((word) => word[0]).join("")}</div></section>
    <main className="store-main"><div className="store-toolbar"><div className="categories">{categories.map((item) => <button className={category === item ? "category-active" : ""} onClick={() => setCategory(item)} key={item}>{item}</button>)}</div><div className="store-search"><Search size={16} /><input placeholder="Search products" value={search} onChange={(e) => setSearch(e.target.value)} /></div></div>
      <div className="product-heading"><div><h2>Shop products</h2><p>{products.length} items available</p></div></div>
      {products.length ? <div className="product-grid">{products.map((listing, index) => { const item = state.inventory.find((entry) => entry.id === listing.inventoryItemId)!; return <article className="product-card" key={listing.id}><div className={`product-image tone-${index % 4}`}>{item.imageUrl?.startsWith("data:") ? <img src={item.imageUrl} alt={listing.title} /> : <div className="product-placeholder"><span>{listing.title.split(" ").slice(0, 2).map((word) => word[0]).join("")}</span></div>}<span className="stock-pill">{item.quantity} left</span></div><div className="product-body"><span className="product-category">{item.category}</span><h3>{listing.title}</h3><p>{listing.description}</p><div className="product-footer"><strong>₹{listing.price}</strong><button onClick={() => { addToCart(listing.id); setDrawer(true); }}><Plus size={16} />Add</button></div></div></article>})}</div> : <div className="empty"><div className="empty-icon"><Search /></div><h3>No matching products</h3><p className="muted">Try another category or search term.</p></div>}
    </main>
    <section className="store-promise"><Sparkles size={22} /><div><strong>Powered by ScanMart AI</strong><span>Product information is reviewed by the seller before publishing.</span></div></section>
    {drawer && <><button className="drawer-overlay" onClick={() => setDrawer(false)} /><aside className="cart-drawer"><div className="drawer-head"><div><h2>Your cart</h2><span>{state.cart.reduce((sum, item) => sum + item.quantity, 0)} items</span></div><button onClick={() => setDrawer(false)}><X /></button></div><div className="drawer-items">{cartDetails.length ? cartDetails.map(({ listing, quantity }) => <div className="drawer-item" key={listing.id}><div className="mini-image">{listing.title.slice(0, 1)}</div><div><strong>{listing.title}</strong><span>₹{listing.price}</span><div className="quantity"><button onClick={() => setCartQuantity(listing.id, quantity - 1)}><Minus size={13} /></button><b>{quantity}</b><button onClick={() => setCartQuantity(listing.id, quantity + 1)}><Plus size={13} /></button></div></div><b>₹{listing.price * quantity}</b></div>) : <div className="empty"><ShoppingBag size={30} /><p>Your cart is empty.</p></div>}</div>{cartDetails.length > 0 && <div className="drawer-total"><div><span>Subtotal</span><strong>₹{total}</strong></div><small>Pay at pickup / cash on delivery</small><Link href="/cart" className="btn btn-primary">Continue to checkout</Link></div>}</aside></>}
    <style jsx>{`
      .store-header{height:72px;max-width:1200px;margin:auto;padding:0 24px;display:flex;align-items:center;justify-content:space-between}.store-header nav{display:flex;align-items:center;gap:24px;font-size:12px;color:#657169}.cart-button{border:0;background:#092922;color:white;border-radius:10px;padding:10px 14px;display:flex;align-items:center;gap:8px;font-weight:750}.cart-button b{background:#73AB95;width:18px;height:18px;border-radius:50%;display:grid;place-items:center;font-size:9px}.store-hero{max-width:1160px;margin:12px auto 0;background:#092922;color:white;border-radius:24px;padding:56px 65px;display:flex;align-items:center;justify-content:space-between;overflow:hidden;position:relative}.store-hero h1{font-size:48px;letter-spacing:-.055em;margin:16px 0 8px}.store-hero p{color:#bdd0c3}.store-kicker{font-size:11px;font-weight:800;color:#7CD4AC;display:flex;gap:7px;align-items:center;text-transform:uppercase;letter-spacing:.08em}.store-meta{display:flex;gap:18px;margin-top:23px;color:#c7d6cc;font-size:11px}.store-meta span{display:flex;align-items:center;gap:6px}.store-mark{width:170px;height:170px;border-radius:50%;display:grid;place-items:center;font-size:55px;font-weight:900;letter-spacing:-.07em;background:rgb(255 255 255 / 8%);border:1px solid rgb(255 255 255 / 12%);box-shadow:0 0 0 35px rgb(255 255 255 / 3%)}.store-main{max-width:1160px;margin:0 auto;padding:34px 0 80px}.store-toolbar{display:flex;align-items:center;justify-content:space-between;gap:20px}.categories{display:flex;gap:7px;flex-wrap:wrap}.categories button{border:1px solid #e1e6e2;background:white;color:#67736b;padding:9px 13px;border-radius:99px;font-size:11px;font-weight:700}.categories .category-active{background:#F6F6F6;color:#2C645B;border-color:#A4B4CC}.store-search{display:flex;align-items:center;gap:8px;border:1px solid #e1e6e2;background:white;border-radius:10px;padding:0 12px}.store-search input{border:0;outline:0;background:transparent;height:38px;width:170px;font-size:12px}.product-heading{margin:36px 0 18px}.product-heading h2{font-size:24px;margin:0;letter-spacing:-.035em}.product-heading p{font-size:11px;color:#7c867f;margin:5px 0}.product-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}.product-card{background:white;border:1px solid #e4e9e5;border-radius:17px;overflow:hidden;transition:.2s}.product-card:hover{transform:translateY(-3px);box-shadow:0 16px 35px rgb(29 54 38 / 8%)}.product-image{height:220px;display:grid;place-items:center;position:relative}.tone-0{background:#edf3ef}.tone-1{background:#f2eee9}.tone-2{background:#eaf0f3}.tone-3{background:#f2eef3}.product-image img{width:100%;height:100%;object-fit:cover}.product-placeholder{width:105px;height:140px;border-radius:25px 25px 14px 14px;background:linear-gradient(145deg,#fff,#d9e7dd);box-shadow:0 16px 25px rgb(35 60 43 / 13%);display:grid;place-items:center;color:#2C645B;font-size:30px;font-weight:900}.stock-pill{position:absolute;top:12px;right:12px;background:rgb(255 255 255 / 88%);padding:5px 8px;border-radius:99px;font-size:9px;font-weight:800}.product-body{padding:18px}.product-category{color:#2C645B;font-size:9px;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.product-body h3{font-size:15px;margin:8px 0}.product-body p{color:#727d75;font-size:11px;line-height:1.55;height:34px;overflow:hidden}.product-footer{display:flex;align-items:center;justify-content:space-between;margin-top:17px}.product-footer strong{font-size:18px}.product-footer button{border:0;background:#F6F6F6;color:#2C645B;border-radius:9px;padding:8px 11px;display:flex;gap:5px;align-items:center;font-weight:800;font-size:11px}.store-promise{max-width:1160px;margin:0 auto 50px;border:1px solid #e1e7e2;border-radius:14px;padding:17px 20px;display:flex;gap:12px;align-items:center;color:#2C645B}.store-promise strong,.store-promise span{display:block}.store-promise strong{font-size:12px}.store-promise span{font-size:10px;color:#758078;margin-top:3px}.drawer-overlay{position:fixed;inset:0;z-index:40;border:0;background:rgb(15 24 18 / 35%)}.cart-drawer{position:fixed;z-index:50;inset:0 0 0 auto;width:min(430px,100%);background:white;display:flex;flex-direction:column;box-shadow:-20px 0 50px rgb(20 30 24 / 14%)}.drawer-head{padding:24px;display:flex;justify-content:space-between;border-bottom:1px solid #e7ebe8}.drawer-head h2{margin:0;font-size:22px}.drawer-head span{font-size:11px;color:#78837c}.drawer-head button{border:0;background:#f2f4f3;border-radius:9px;width:37px;height:37px}.drawer-items{padding:20px;overflow:auto;flex:1}.drawer-item{display:grid;grid-template-columns:56px 1fr auto;gap:12px;padding:14px 0;border-bottom:1px solid #edf0ee}.mini-image{width:56px;height:62px;border-radius:10px;background:#edf3ef;display:grid;place-items:center;color:#2C645B;font-weight:900}.drawer-item strong,.drawer-item span{display:block;font-size:12px}.drawer-item span{font-size:10px;color:#78837c;margin-top:5px}.quantity{display:flex;align-items:center;gap:10px;margin-top:10px}.quantity button{width:25px;height:25px;border:1px solid #e0e5e1;background:white;border-radius:6px}.quantity b{font-size:11px}.drawer-total{border-top:1px solid #e3e8e4;padding:22px}.drawer-total>div{display:flex;justify-content:space-between}.drawer-total small{display:block;color:#79847d;font-size:10px;margin:8px 0 17px}.drawer-total .btn{width:100%}
      @media(max-width:900px){.store-hero,.store-main,.store-promise{margin-left:16px;margin-right:16px}.product-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:600px){.store-header nav>a{display:none}.store-hero{padding:35px 26px}.store-hero h1{font-size:36px}.store-mark{display:none}.store-toolbar{align-items:stretch;flex-direction:column}.store-search input{width:100%}.product-grid{grid-template-columns:1fr}.product-image{height:250px}}
    `}</style>
  </div>;
}


